Volume is {{ volume }} l ({{ volume | l2gal | round | int }} imperial gallons).

The number is {{ number }}.

The colors are: {% set comma = joiner(", ") %}{% for COLOR in colors %}{{ comma() }}{{ COLOR }}{% endfor %}.

{{ content }}

